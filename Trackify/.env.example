# This is a template for your actual .env file
# Make sure to copy this file as `.env` inside the /Trackify directory

# MongoDB connection string
# To get your MongoDB URI:
# 1. Go to https://cloud.mongodb.com and log in (or sign up if you’re new).
# 2. Create a new project and then a new cluster (choose the free tier for testing).
# 3. Create a database user with username and password, then click on "Choose a connection method"
# 3. OR after the cluster is deployed, click “Connect” → “Connect your application”.
# 4. Copy the provided connection string (it looks like: mongodb+srv://<username>:<password>@cluster.mongodb.net/<dbname>)
# 5. Replace <username>, <password>, and <dbname> with your actual MongoDB Atlas credentials.
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/dbname

# Secret key for express-session (keep it long and random in real .env)
SESSION_SECRET=your-super-secret-key

# Gmail email address to send reminders from
EMAIL=your-email@gmail.com

# Gmail App Password (16-digit key, not your actual email password!)
EMAIL_PASSWORD=your-16-digit-app-password
# You can generate one from your [Google Account – App Passwords](https://myaccount.google.com/apppasswords).
